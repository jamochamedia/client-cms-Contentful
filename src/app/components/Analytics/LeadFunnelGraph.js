import styled from "styled-components";
import React from "react";

const Card = styled.div`
  background-color: white;
`;

function LeadFunnel(props) {
  return (
    <div>
      <Card>Lead Funnel</Card>
    </div>
  );
}

export default LeadFunnel;
